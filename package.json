{
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright test --reporter=html && npx playwright show-report"
  },
  "devDependencies": {
    "typescript": "^4.1.3",
    "ts-node": "10.0.0"
  }
}
