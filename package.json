{
  "scripts": {
    "register": "npx playwright test reg.spec.js ",
    "saveauth": "npx playwright test login.spec.js --save-storage=auth.json",
    "test": "npx playwright test",
    "windowtest": "npx playwright test --config=window.config.ts",
    "mactest": "npx playwright test --config=macos.config.ts",
    "linuxtest": "npx playwright test --config=linux.config.ts",
    "report": "npx playwright test --reporter=html && npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.29.2",
    "ts-node": "10.0.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "csv-parser": "^3.0.0"
  }
}
